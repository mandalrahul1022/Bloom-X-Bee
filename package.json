{
  "name": "phenology-10-places",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "serve": "node server.js",
    "api": "python -m uvicorn backend.main:app --reload --port 8000 --reload-dir backend --reload-exclude 'node_modules/*'",
    "start": "npm-run-all --parallel serve api",
    "dev": "npm-run-all --parallel serve api"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.5",
    "npm-run-all": "^4.1.5"
  }
}